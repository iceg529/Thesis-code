To download the models, run the python script as

python download_models.py

and then unzip the file . A folder 'models' with all trained networks will be downloaded.

After downloading, evaluate our models on sample image pairs with script from folder 'test'. Example images are also available.

Refer to individual folders for other README files.
